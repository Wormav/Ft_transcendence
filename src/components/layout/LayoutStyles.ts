export const LayoutStyles = {
	container: 'min-h-screen w-full flex flex-col bg-background font-sans relative',
	contentWrapper: 'w-full h-full',
	overlay:
		'absolute inset-0 bg-black/5 backdrop-blur-sm transition-opacity duration-300 ease-in-out pointer-events-none z-10',
};

export default LayoutStyles;
